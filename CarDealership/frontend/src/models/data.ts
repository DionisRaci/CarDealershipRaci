export interface IUser{
  userId: number,
  username: String,
}

export interface ICar{
  price: number,
  carID: number,
  carModel: string,
  carProducer: string,
  isBooked:boolean,
  isBought:boolean
}
